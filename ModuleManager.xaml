<Window x:Class="RetailCorrector.RegistryManager.ModuleManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns:l="clr-namespace:RetailCorrector.RegistryManager"
        Title="Менеджер модулей" Height="450" Width="890">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="22"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <xctk:WatermarkTextBox Watermark="Поиск"/>
        <xctk:WatermarkComboBox Grid.Column="2" ItemsSource="{Binding Source={x:Static l:RegistryList.Registries}}" 
                                Watermark="Реестры модулей не найдены" SelectedItem="{Binding CurrentRegistry}"/>
        <ScrollViewer Grid.Row="2" Grid.ColumnSpan="3" Margin="-5"
                      VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Disabled">
            <ScrollViewer.Content>
                <ItemsControl ItemsSource="{Binding Modules}" Height="Auto">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Child="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer.Content>
        </ScrollViewer>
    </Grid>
</Window>
