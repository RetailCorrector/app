<UserControl x:Class="RetailCorrector.Wizard.ModuleInfo"
             Width="Auto" Height="Auto" Background="{x:Null}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             BorderBrush="Gray" BorderThickness="1,0,1,1"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="2.5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Text="{Binding Package.Name}" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="2" Text="{Binding Package.Description}" VerticalAlignment="Center" TextWrapping="WrapWithOverflow"/>
        <Button Content="{Binding InstallText}" Grid.Column="4" VerticalAlignment="Center" IsEnabled="{Binding CanClick}" Click="Install"/>
    </Grid>
</UserControl>
