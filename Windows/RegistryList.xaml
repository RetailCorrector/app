<Window x:Class="RetailCorrector.Wizard.Windows.RegistryList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns:w="clr-namespace:RetailCorrector.Wizard.Windows"
        WindowStyle="ToolWindow" ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Title="Менеджер реестров" Height="200" Width="375">
    <Window.Resources>
        <Style TargetType="Path">
            <Setter Property="Fill" Value="Black"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Canvas DockPanel.Dock="Right" Width="25" Margin="5">
            <Button Width="25" Height="25" Click="Add">
                <Path Data="M13 11h-2v3H8v2h3v3h2v-3h3v-2h-3zm1-9H6c-1.1 0-2 .9-2 2v16c0 1.1.89 2 1.99 
                      2H18c1.1 0 2-.9 2-2V8l-6-6zm4 18H6V4h7v5h5v11z"/>
            </Button>
            <Button Width="25" Height="25" Canvas.Top="30" Click="Delete">
                <Path Data="M14.12 10.47L12 12.59l-2.13-2.12-1.41 1.41L10.59 14l-2.12 2.12 1.41 1.41L12 
                      15.41l2.12 2.12 1.41-1.41L13.41 14l2.12-2.12zM15.5 4l-1-1h-5l-1 1H5v2h14V4zM6 19c0 
                      1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM8 9h8v10H8V9z"/>
            </Button>
        </Canvas>
        <DataGrid ItemsSource="{Binding Source={x:Static w:RegistryManager.Registries}}" SelectedIndex="{Binding SelectedIndex}" 
                  HeadersVisibility="None" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Binding="{Binding Value}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
